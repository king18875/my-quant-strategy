name: Quant Strategy CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PYTHON_VERSION: "3.10"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Python version
        run: |
          python --version
          pip --version

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

          - name: Install Dependencies
      run: |
        pip install backtrader akshare pandas numpy quantstats openpyxl

      - name: Run strategy
        run: |
          python my_strategy.py --notify
        env:
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: reports/
